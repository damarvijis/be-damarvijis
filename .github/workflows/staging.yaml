name: Deploy to staging
on:
  workflow_dispatch: {}
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: 'Checkout repository'
      - uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            # echo "Nice Job!!!"
      # - run: ssh damarvijis@damarvijis.com "cd /var/www/app-be-stg && pm2 restart app-be-stg || pm2 start --name app-be-dev node index.js --port 8081"
      
